<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/navbar.css">
    <link rel="stylesheet" href="/background.css">
    <link rel="stylesheet" href="/cards.css">
    <title>Your Watchlist</title>
</head>
<body class="background">
    <header>
        <div class="navbar">
            <a href="/auth/menu" class="menu-btn">&#9776;</a>
            <div class="nav-btn">
                <ul>
                    <li><a href="/auth/index">Home</a></li>
                    <li><a href="/auth/cards">Watchlist</a></li>
                    <li><a href="/transfers">My Ratings</a></li>
                    <li><a href="/reviews">Forum</a></li>
                    <li><a href="/logout">Log Out</a></li>
                </ul>
            </div>
        </div>
    </header>

    <div class="container my-4">
    <h1>Your Watchlist</h1>

    <div class="row">
        {{#each movies}}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <!-- Use fallback image if poster_path is empty or broken -->
                    <img src="https://image.tmdb.org/t/p/w500{{poster_path}}" class="card-img-top" alt="{{title}}" 
                         onerror="this.onerror=null;this.src='/images/default-poster.jpg'">
                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text text-truncate">{{overview}}</p>
                        <a href="/auth/movies/{{id}}" class="btn btn-primary">View Details</a>
                        <form action="/auth/remove-from-watchlist" method="POST" class="mt-2">
                            <input type="hidden" name="movie_id" value="{{id}}">
                            <button type="submit" class="btn btn-danger">Remove from Watchlist</button>
                        </form>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>

</body>
</html>
