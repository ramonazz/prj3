<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/navbar.css">
    <link rel="stylesheet" href="/background.css">
    <link rel="stylesheet" href="/cards.css">
    <title>Search Results</title>
</head>
<body class="background">
    <div class="container my-4 glass">
        <!-- Search Results Content --><div class="container my-4 glass">
    <h1 class="text-center mb-4">Search Results for "{{query}}"</h1>

    {{#if message}}
        <div class="alert alert-info">{{message}}</div>
    {{/if}}

    <div class="row">
        {{#each movies}}
            <div class="col-md-4 mb-4">
                <div class="card h-100 movie-card">
                    <img src="https://image.tmdb.org/t/p/w500{{poster_path}}" class="card-img-top" alt="{{title}}">
                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text text-truncate">{{overview}}</p>
                        <a href="/auth/movies/{{id}}" class="btn btn-primary">View Details</a>
                        <form action="/auth/add-to-watchlist" method="POST" class="mt-2">
                            <input type="hidden" name="movie_id" value="{{id}}">
                            <button type="submit" class="btn btn-secondary">Add to Watchlist</button>
                        </form>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>

    </div>
</body>
</html>


